import 'package:flutter/material.dart';

class ColorFieldWidget extends StatelessWidget {
  const ColorFieldWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
