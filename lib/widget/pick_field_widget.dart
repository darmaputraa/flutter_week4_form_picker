import 'package:flutter/material.dart';

class PickFieldWidget extends StatelessWidget {
  const PickFieldWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
